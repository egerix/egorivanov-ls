@import "../../../styles/mixins.pcss";
.skills {
    display: flex;
    flex-wrap: wrap;
    margin-left: -30px;
    margin-top: -30px;

    @include tablets {
        flex-direction: column;
    }
}

.item {
    flex-basis: calc(100%/2 - 30px);
    margin-left: 30px;
    margin-top: 30px;
}