.userinfo {
  display: flex;
  flex-direction: column;
  text-align: center;
}

.userinfo__title {
  font-size: 18px;
  line-height: 30px;
  color: rgba($text-color, .5);
  font-weight: 600;
  margin-bottom: 24px;
}

.userinfo__name {
  font-weight: 800;
  font-size: 72px;
  line-height: 60px;
}