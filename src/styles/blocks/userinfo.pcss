.userinfo {
  display: flex;
  text-align: left;
}

.userinfo__title {
  font-size: 18px;
  color: rgba($text-color, .4);
  font-weight: 600;
  text-align: right;
  margin-bottom: 0;
  padding-top: 8px;
  margin-right: 5%;
}

.userinfo__occ {
  white-space: nowrap;
}

.userinfo__name {
  font-weight: 800;
  font-size: 72px;
  line-height: 1;
}